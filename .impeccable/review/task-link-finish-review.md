disposition: ship

Substituted in-thread finish check: a fresh reviewer could not be started because the session's agent thread limit was reached. This is not an independent-review claim. No approved comp or quality-bar image applies to this precisely specified local extension; root PRODUCT.md/DESIGN.md remain absent rather than being invented for this change.

## persistence

Pass at local-extension scope. Direction is recorded in `../task-link-direction.md` and the registered surface brief. The incumbent authority is `../delivery-design.md`. Post-build documentation is scoped to the task-link extension, not a replacement global identity. No shipping raster was created. Association data is explicitly session-local, with the refresh-loss notice in the dialog footer.

Required captures inspected: `task-link-desktop.png` (1440×1000), `task-link-mobile.png` (390×844), `task-link-user-816.png` (816×800); supplemental `task-link-new-mobile.png` and `task-link-toast-mobile.png`. Two intermediate captures taken before viewport resizing settled were invalid and were replaced; they are not evidence for this verdict.

## fidelity

| Element / promise | Disposition | Evidence |
| --- | --- | --- |
| TYPE | match | Inherited sans; 18px title, 13px controls/body, 12px context and compact monospace IDs. No global scaling. |
| MATERIAL | match | Flat white working surface and official Phosphor Regular icons. Missing preview assets are labelled, not fabricated. |
| GROUND | match | Uses incumbent panel, subtle, accent-soft and backdrop values; no palette replacement. |
| THESIS | match | User selects the delivery slot explicitly; branch creation does not silently create a delivery Item. |
| STORY | match | Detail entry → sheet → name/ID search → version consequence → acknowledgement when replacing → association. |
| FIRST VIEWPORT | adaptation | 600px dialog with protected footer. The body can scroll, and results have a separate capped list; on a short phone viewport the acknowledgement may require body scrolling. This keeps all local entries reachable and the action/validation visible; the actual implementation is documented rather than claiming one scroll region. |
| FORM | match | Native dialog/select, explicit focus and disabled reasons, version/history guards. Seed/comp generation is inapplicable to the recorded precisely specified extension. |
| Item identity | match | Target name and ID are retained when updating; source Item/Run/version are recorded separately. |
| Branch new Item | match | Explicit mode, nonempty unique name, generated new ID, v1, source/mainline unchanged. |
| Feedback | match | Distinct success, candidate/info and warning states; Toast above modal layers, dismissible and pausable. |

## ceiling

Reached for this compact Operate extension. No additional imagery or motion is required for the selection mechanism. Actual thumbnails remain dependent on real preview data; the current seeded dataset contains no preview URLs. Background aggregate totals exceed the local sample catalog and are not represented as a fully loaded backend list.

## material_fixes

None remain in the scored correction list:

1. Resolved — rejected linked candidates without an existing final are no longer reusable as ready sources from delivery details; list/picker copy identifies rework. The existing approved final remains reusable. Covered by the added regression test.
2. Resolved — dynamic native sheet options now have explicit accessible names. The final browser snapshot exposes each sheet name instead of unnamed options.

Verification: 89 automated tests passed, including 16 association tests. Independent test-tab interaction covered replacement, duplicate blocking, empty search, branch Item creation and aggregate deltas. Desktop, phone and reported-width layouts were inspected. The detector ran once and returned no findings in degraded regex mode; this is not a full computed-style audit. The historical run summary's generic “待审核” label is outside this extension and is not treated as proof of the source's review status.

## keep

Keep stable delivery IDs, preserved version history, explicit replacement confirmation, truthful missing previews and the visible local-demo boundary.
