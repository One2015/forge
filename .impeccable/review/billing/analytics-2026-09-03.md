# 用量与费用 · 信息架构与分析复核

范围：现有本地 Forge 页面；保留设计系统、示例数据边界及项目／供应商／模型维度。未部署、未提交真实账单操作。

## 已完成

- 两层导航与筛选：四个维度入口；一行日期／项目／供应商／模型，条件式重置。
- 四项非重复 KPI；等长上一周期对比、调用量／单次成本变化分解、费用增减来源。历史覆盖不足不假设为零。
- 单一主图切换费用、输入／输出堆叠 Tokens、调用次数；Radix hover／focus 明细；零值桶；点击时段同步筛选且显示可清除标签。
- 费用构成 Top 5、其余合计；数值右对齐与 tabular numbers；七列双向排序、行下钻、返回上一级、搜索、分页、两类 CSV。
- KPI 附近的计费说明；时间／维度／图表／排序／搜索／分页与 URL 联动。

## 复核证据

- 自动化覆盖：账单边界与完整性、跨维度总额、缺失覆盖、零基期、费用变动分解、已停止项目降幅归因、时段联动、排序、搜索、分页、CSV 精度／转义／公式防护、下钻返回、路由 round trip。
- 浏览器实测：项目 → 供应商 → 模型及返回、三项联动选择、快捷／自定义日期、鼠标和键盘重置、Token 模式 hover tooltip、柱形点击与清除、升序表格、模型分页、搜索。
- 导出操作在页面确认生成：搜索后的 1 条汇总 CSV、完整筛选下的 18 条调用 CSV；测试验证导出的实际数据内容与精度。
- 1280px、390px、320px 无 document 横向溢出。图表和 950px 明细表在各自容器滚动。320px KPI 单列；日期浮层边界 x=16…304、y=180…560，计费说明 y=129…611，均位于 320×740 视口内。
- 本轮修正了模板组合条件导致筛选标签不展示、动态标题无障碍名称滞后、极窄屏日期浮层裁切。未引入新依赖或动画。

限制：当前仍为明确标注的本地示例账单；未接入生产分页／费用阈值／预算，不把增长自动宣称为异常。数据源未提供历史覆盖时，不计算环比。
