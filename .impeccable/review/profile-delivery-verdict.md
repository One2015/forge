# Profile delivery / Skill refinement · verification

Disposition: ship the requested local prototype update.

- Member defaults to 一万, with no identity-editing affordance or role/scope disclosure. Lead / Owner may edit their current demo identity; Owner cannot promote itself to Lead. Renaming preserves the stable account key.
- Profile tasks are delivery sheets only. Clicking the Member's 蚂蚁 Web3D sheet opens its detail with clean filters, without starting a review or listing individual cases in the profile.
- Empty Skill library retains an upload button. Unit tests exercise multiple MD uploads, ZIP parsing, naming, command validation, partial failures, cancellation, stale reads, capacity, account isolation and sheet binding. Browser QA uses an isolated parsed-upload fixture; it does not claim an OS file-picker interaction.
- Browser checks exercised whitespace-name validation/disabled Save, named Skill saving and binding, personal vs bound snapshots, Lead name editing and downgrade to Member, and disappearance of the edit affordance.
- No-bound delivery detail has zero visible Skill sections; the bound fixture exposes the Skill again. Both workbench and sheet markup are covered by bound-availability regression checks.
- At 1272 × 800 the editor is 400px wide and its Save action is visible. At 390 × 844 the panel is 306px wide, bounded at x=72…378, with no inner horizontal overflow and 44px editable controls. Ordinary lists keep the compact height.

One bounded visual correction increased only the inline editor's available scroll height. Existing typography, colors, rail, review outcomes, and branch/rework workflows are preserved. No motion or shipping raster assets added. Screenshots are QA evidence only.

Verification: `node --test scripts/test-*.mjs` — 113 passed; `npm run build` — passed. Temporary fixture is generated by `scripts/create-profile-qa-fixture.mjs` and removed after inspection. All behavior remains in local memory; role gates are not server authentication, and Skill loading is still a demonstration, not a real evaluation engine.
