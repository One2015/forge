# Overview summary

Mode: Operate. Refine the existing neutral Forge UI, keeping five compact summary cards in the order 待审核、运行中、交付缺口、近 24 小时成本、模型状态. Review remains scoped to the current user's assignments; omit the visible 指派给你 label beside the title, retaining scope in the info tooltip without changing the count or destination.

- Preview the first two actual pending Item titles, using the existing Item-title formatter; append 等 only when there are more. Do not combine sheet names and dataset groups as fictional “projects”. Keep long previews to two visual lines, with full text retained in the DOM. The upper-right meta area contains only the info control.
- Running's main number counts Run tasks; supporting copy counts processing Items. Queued work is excluded. Delivery shortage sums positive target-minus-deliverable quantities, never quality failures or overdue sheets. Distinguish no sheets from all quantities complete.
- Cost sums the recorded cumulative fees for Runs started within the last 24 hours. It is neither natural-day spend nor a usage-event billing window; expose that distinction in help text. Do not expand this into billing without usage-event data.
- The first three cards retain their existing destinations. Cost remains informational. Model status opens its dedicated read-only detail view, including when the monitoring source is not connected.
- Each card has a named Phosphor info control with a Radix tooltip supporting hover, focus, Escape and touch activation. Tooltip roots live inside the standalone document, not the parent iframe host. Preserve the native title fallback.
- Model status displays `% 当前可用` using the same source selection and evidence-aware aggregation as the detail page. With monitoring absent, the user-requested mock snapshot is visibly labelled 示例数据: 5 / 5 available models, 5 issues to handle, and highest latency 3.5× across all routes, independent of detail-page filters. The help text identifies the snapshot as simulated; 100% generation availability does not imply healthy latency, quality or balance. An explicit live-source selection still shows the disconnected state. Real monitoring takes priority, including loading, errors and expired evidence, and is never replaced by mock data. Deduplicate production-enabled models; a fresh passing, enabled, routable and protocol-matched route confirms availability. Incomplete/invalid/stale catalogs and unknown model states withhold the percentage. Bare legacy counts do not prove current availability. Preserve live numerator/denominator, routes needing attention and recent generation-check time. No quality composite score.
- Keep the established 12px labels and 25px figures; responsive layout switches from five columns to two and then one, without shrinking type.
- Delivery-progress rows use the sheet's saved customer/supplier Logo in a fixed 28px identity slot instead of a colored status dot. With no Logo or a failed image, show the supplier's first character in the existing neutral treatment; never invent brand artwork. Fit images without cropping, keep supplier text adjacent, and preserve quantities, shortage copy, and row navigation. Ignore late failures from a replaced or removed Logo.

The existing 阶跃 and 蚂蚁 orders use the user's supplied images, preserved without redrawing or cropping at `public/supplier-logos/stepfun.png` and `public/supplier-logos/ant.png`. Custom sheet uploads and explicit removal override these seed defaults. The shared sheet logo feeds the overview and delivery editor.

Sources: `scripts/templates/overview-summary.*`, `scripts/templates/overview-summary-methods.js`, `scripts/templates/overview-delivery-identity.html`, `scripts/ui/summary-tooltips.tsx`. Apply the focused generator, then the normal build script. Supplier defaults live in `delivery-methods.js`; synchronize with `update-delivery-skill-workspace.mjs`.

Validation: source/model regression checks passed. Browser verification and public publication are pending user approval for this change set.
