# 本地验证记录 · 2026-09-02

## GitHub main 合并前验证 · 2026-09-04

- `npm run build` 通过，当前入口与生成资源已重新构建；Vinext 的路由静态分类提示仍为非阻塞提示。
- `npm test` 全部 546 项通过，无跳过。更新了一条查看器旧断言：统一详情页显示空态文字，嵌入式查看器保留屏幕阅读器状态；文件预览、下载和加载恢复检查保留。
- `git diff --check` 通过。此次验证用于将当前工作区提交并合并到 GitHub main，不代表生产后端或飞书服务已接入。

## 当前入口与旧版清理 · 2026-09-04

- 删除旧版公开 HTML、局部字体迁移脚本、字体、组件展示页及专用文档；当前界面仍使用系统字体和既定 Postman 工作台样式。
- 业务基础模板移至 `scripts/templates/forge-base.html`，由当前生成器组装 `public/forge-postman.html`。默认 dev/build 与兼容命令统一构建路由、查看器、Tooltip 和当前页面。当前仍使用的运行页原生控件提取为 `public/postman-ui/primitives.css`；加载按钮防重复操作的既有测试随控件保留。
- 内部模板和生成页面的业务逻辑与清理前逐字一致。应用、脚本、当前资源和设计文档已无已删除资源的引用。`npm run build`、`git diff --check` 通过。
- `npm test` 清理前 548 项中 547 通过，清理后 543 项中 542 通过：移除 5 项专属于旧迁移/旧样板的测试。唯一失败前后相同，为 `test-artifact-preview.mjs` 的 “empty canvas has no visible placeholder or file heading but retains accessible status and real file actions”；本次未修改该既有断言或查看器业务。
- 浏览器实测概览、生产运行、审核、交付入口；运行筛选无匹配与重置恢复正常。运行页 23 个关键元素清理前后的几何位置、字体、颜色和间距一致；390px 窄屏文档宽度为 390px。此次是清理回归抽查，不替代全站状态审查。

## 最新：创建数据单四步向导 · 2026-09-03

- 创建页改为基础信息 / 导入 List / 配置规则 / 确认创建，保留已有编辑弹窗和 Forge 设计系统。创建流程不提供保存草稿或自动保存；已有存储未删除。
- 全量 416 / 416 测试通过；生产构建通过。新增 15 项四步门禁、输入保留、解析/重复/异常、分页、互斥导入、Tag 默认与覆盖、Skill、成员、账号变化和最终保存回归。既有 ZIP/Logo/Skill 上传校验仍有自动化覆盖。
- 浏览器 1280×900 和 390×844 验收：桌面 sticky 摘要、移动端折叠摘要、步骤定位、表格和底栏无可见横向溢出；实际操作确认异常筛选和删除、粘贴/ZIP 切换保留、数量警告和显式调整、Tag 自动选择/单条覆盖、Skill 整行选择/搜索/类别/置顶/详情，以及成员搜索/角色/唯一所有者保护。
- 最终“创建数据单”浏览器点击被安全审查阻止（可能新增记录并发送成员提醒），未重试或绕过。最终创建逻辑通过自动化验证，但实际浏览器提交未完成。验收表单已退出，视口已恢复，未创建测试记录或发送消息，未部署。
- 实现与约束见 `.impeccable/delivery-create-design.md`。

## 最新：昨日成本与用量分析 · 2026-09-03

- 概览卡片改为「昨日成本」，进入总览／供应商／模型分析。统一 UTC+8 自然日、USD 事件账单口径；示例数据在卡片和页面同时明确标注，真实数据源未接入。
- `npm test` 350 / 350 通过；其中 16 项费用回归覆盖日期边界、跨年／闰日、四种粒度、三种维度一致性、筛选、下钻、排序、空／加载／错误／不完整数据、重复记录、非法金额与同步幂等。`npm run build`、`git diff --check` 通过。
- 独立检查提出「近 12 个月」可能跨 13 个自然月；已改为最近 12 个完整自然月。新增 6 组日期边界场景，浏览器验证 2025-09-01 至 2026-08-31 恰好 12 个按月区间。
- 本地 3007 浏览器验证：昨日卡片与分析总额相同；4 个项目、5 家供应商、6 个模型的合计一致；项目下钻保留日期并更新模型；原生供应商选择会更新金额；近 7 天预设正确选择日粒度；开始日期晚于结束日期显示错误，可恢复昨日范围；小时／日／月／年切换保留总额；费用和 Token 排序有效。
- 浏览器实测柱形 Enter 选择显示精确费用与输入／输出 Token，键盘为 2px 橙色焦点；返回入口恢复概览，进入时聚焦标题。修复了模板属性三元表达式无效、动态 table foster-parenting 及日期 input 事件兼容问题。
- 1440px、用户 919×802 与 390px 窄屏检查：日期、下拉与粒度按钮在手机为 44px 高；页面没有水平溢出，890px 明细表在局部容器横向滚动。截图见 `.impeccable/review/billing/`，分别保留顶部和下半页视口图；浏览器全页拼接失真，因此未将拼接图作为验收证据。
- Impeccable 定向检测一次返回 `[]`，但解析依赖缺失，降级为正则模式；这不是计算对比度或全局无障碍认证。独立复核最终为 `ship`，范围仅覆盖日期范围及末尾刻度重叠两项修正，均已解决；记录见 `.impeccable/review/billing/review.md`。
- 本次没有审核／返工／运行提交、账单支付、充值、后端接入或公开部署；只更改本地前端与合成示例。生产接入要求详见 `scripts/BILLING_ANALYTICS.md`。

## 最新：返工提交 hover 覆盖防护 · 2026-09-03

- 当前 3007 版本修改前实测可启用的提交 hover 为深橙底白字，未复现用户截图中的浅底白字。源码仍让主按钮同时命中通用灰底 hover 规则，因此本轮做定向加固：灰底 hover / pressed 显式排除 primary 和暖橙 secondary；primary hover 同时声明白色文字、深橙底色及边框，不依赖后置规则覆盖灰底。
- 新增中性选择器排除语义主按钮的回归；`npm test` 为 334 / 334 通过，`npm run build`、`git diff --check` 通过，Impeccable 定向 CSS 检测返回 `[]`。
- 修改后浏览器确认：空白说明时 disabled + hover 保持 `rgb(238,239,235)` 灰底与 `rgb(129,135,123)` 灰字；填写后启用时为橙底白字；键盘焦点 2px 橙色轮廓；390px 下按钮高度 44px，无水平溢出。此前测试草稿均已取消，没有提交返工或运行。
- 最后一次已启用 hover 复测未完成：跨调用指针状态读数为 `hover:false`，不能当作 hover 验收；随后再次填写临时草稿的操作被安全限制拦截，未重试或绕过。最终补测需用户明确允许临时填写并取消草稿，保持不提交。

## 最新：审核 repair / Reroll CTA · 2026-09-03

- 按 Impeccable 统一入口 CTA：浅橙底、暖橙文字、500 字重，队列 13px / 20px；工作台 Reroll 随底栏使用 14px。移除队列两处和工作台一处箭头，保留原标签与 handler；继续填写返工说明沿用同样式。
- `npm test`：333 / 333 通过；新增 3 项共享样式、原生按钮、状态/尺寸、无箭头与定向同步幂等回归。更新原有测试对箭头前空格及图标数量的断言。`npm run build` 和 `git diff --check` 通过；定向 Impeccable CSS 检测返回 `[]`。
- 本地 3007 浏览器：repair 与 Reroll 实际均为 `500 13px / 20px`，前景 `rgb(196,67,24)`，底色 `rgb(255,241,234)`，无 SVG 箭头；hover 为 `rgb(255,230,218)` / `rgb(168,56,18)`。Tab 聚焦显示 2px 橙色轮廓，Enter 正常进入审核。
- 390px 窄屏点击区为 80×44px，无水平溢出。Reroll 点击进入生产 Pipeline 选择，测试配置已取消，未发起运行、返工或审核提交；恢复原预览入口。工作台 Reroll 分支与 pressed/disabled 由源码回归覆盖，未扩大为全局交互审查完成。更新脚本：`node scripts/update-review-cta.mjs`。

## 最新：Item 详情底部操作颜色与固定关闭按钮

- 按 Impeccable 的 Operate 色彩层级保留次要操作语义：追加返工为浅橙底、暖橙文字及边框，下载为浅灰底和次级灰图标。关闭改为原生 button，白底、深色图标和轻阴影，固定在滚动区之外。
- `npm test`：330 / 330 通过（包含工作区其他既有测试）。新增 4 项原生关闭、状态层叠、色彩对比和定向同步幂等回归；`npm run build` 与 `git diff --check` 通过。正文/图标所用默认、hover、pressed 色彩组合均达到 4.5:1。按下态为源码与色彩测试，不声称实测持续鼠标按住。
- 复用本项目 3007 服务，直接打开同一 iframe 资源 `/forge.html` 验证莫高窟详情：追加返工实际前景 `rgb(196,67,24)`、底色 `rgb(255,241,234)`；下载前景 `rgb(102,107,97)`、底色 `rgb(246,246,243)`；关闭 hover 底色 `rgb(240,241,237)`。右栏滚动从 0 到 189px 后，关闭按钮 y 保持 40px。
- 390px 窄屏 DOM 检查：关闭目标 44×44px，底部栏不溢出；Enter 可关闭详情。已恢复 viewport 和原预览入口，未提交审核、返工或下载。未在 3001 端口验收或公开发布。
- 本轮仅修复所选按钮：旧交互审查中的整个模态焦点隔离和其他页面控件不在本次完成范围内。

## 最新：创建分支默认继承来源 Item

- `npm test`：323 / 323 通过；覆盖莫高窟未包含在数据集选择列表时的来源预选、手动更换、跨数据集隔离、取消重开、同值选择不清空，以及父分支配置继承。`npm run build` 与 `git diff --check` 通过。
- 本地 `http://localhost:3007/forge.html` 浏览器实测：阶跃 Web3D → 莫高窟 → 第 2 轮 → 从 Run 3 创建独立分支。展开配置后 Item 原生下拉框的实际选中值为来源 ID，显示“莫高窟 · e2b8c4a0f6d3951e7b0c3a6f9d2b5e8a”，摘要显示 Item 莫高窟，无配置错误。
- 仅默认值、选项绑定及来源范围校验变更，未改全局数据集或真实运行状态。浏览器不提交分支；本轮未公开发布。定向同步：`node scripts/update-branch-default-item.mjs`。

## 最新：交付 List / Tag / Skill UI 优化

- 全量 `npm test`：307 / 307 通过。本次新增 8 项 Tag/Skill 状态、快照隔离、空调用名、失效回调、弹窗结构与文本安全回归；测试总数包含工作区其他已有回归。
- `npm run build` 成功；生成的 DCLogic 通过 `new Function` 语法校验，`git diff --check` 通过。类方法模板不是独立 JS 模块，语法验证以合成后的组件为准。
- Impeccable 仅执行一次定向 CSS 检测，返回 `[]`；不把它视作浏览器或无障碍认证。
- 经用户允许，在本地 `http://localhost:3007/forge.html` 实测默认 919×802 与 390×844 窄屏。嵌入式页面的自动化导航出现未触发状态变化的情况，因此交互验收直接打开同一 iframe 资源；未声称已验证所有外围导航。
- ZIP 上传 CTA 实测约 90×32px；窄屏 90×44px。Tag 初始折叠、展开后无预选色，显式选绿后显示选中，输入和 Esc 取消正常；窄屏颜色点击区 44×44px，无横向溢出。实际文件选择/ZIP 上传未重复执行，读取与错误路径由既有测试覆盖。
- 三条平台 Skill 未选中时计算背景均为透明。查看打开 native `:modal` 详情，未改变已选 0 / 12；名称下展示 `/3d trajectory`，完整内容可读。关闭按钮 hover 背景计算为 `rgb(240, 241, 237)`；Esc 关闭后焦点返回对应 查看 按钮。新控件的按下/禁用样式和快捷键隔离有源码/逻辑覆盖，不把它扩大为全局交互问题已修复。
- 390px 窄屏详情内容 `scrollWidth === clientWidth === 315px`；关闭目标 44×44px。修复插值 span 误继承辅助文字样式后，标题实际为 600 字重、深色，只有 Skill 类型标记为次级灰。
- 本次只做本地修改，无数据单提交、审核操作、真实 Skill 发布或公共站点更新。Tag 测试草稿已取消；临时 viewport 恢复默认。

## 此前：交付预览精简快照

| 检查 | 结果与范围 |
| --- | --- |
| `npm test` 等价全量测试 | 286 / 286 通过；新增 3 项预览精简回归，检查历史标题、空态、桌面/移动间距和迁移幂等；更新产物测试以确认示例/本地模型入口已移除，以及只隐藏候选标题标签。保留 21 项模型状态与创建页、平台 Skill、供应商 Logo、概览、成员、通知、排序、侧栏、预览、审核回归 |
| 定向 TypeScript | `scripts/ui/artifact-preview.tsx`、`scripts/ui/model-viewer.ts` 严格检查通过；不是全项目类型检查 |
| `npm run build` | 成功；重新生成 Tooltip、产物查看器、模型库、解码资源以及 Vinext 输出 |
| `git diff --check` | 通过 |
| Impeccable 状态审查 | 本轮为预览精简的源码/回归检查，无独立视觉评分；既有模型页三项独立复核记录仍见 `.impeccable/review/model-status-source-review.md`，不扩大其结论范围 |
| Impeccable 自动检测 | 本轮仅一次，针对产物组件、CSS 和历史标题；解析模块缺失，降级正则扫描未命中。不能视作计算样式、对比度或布局审计；其后仅补充历史空态并清理旧测试 |
| 浏览器视觉/交互 | 本轮未执行：已询问本地浏览器验收权限，尚待确认；自动化逻辑/模板测试不等同布局、原生文件选择或拖拽实测 |
| 公共站点 | 未更新；源码包比公开参考页面新 |
| 本地预览服务 | `http://localhost:3001/` 响应头检查返回 HTTP 200；复用已有服务，不等同浏览器验收 |

定向类型检查命令：

```sh
node node_modules/typescript/bin/tsc --noEmit --skipLibCheck --strict --allowJs --jsx react-jsx --lib dom,esnext --moduleResolution bundler --module esnext --target es2022 scripts/ui/artifact-preview.tsx scripts/ui/model-viewer.ts
```

`npm run build` 当前会提示 Vinext 无法静态分类部分路由，但构建正常结束。没有把此提示当成运行失败。

## 人工验收建议

1. 桌面收起侧栏，悬停伸缩图标，确认立即展开且内容不横移；移动到导航项后不收回；移出后收回。
2. 临时展开时点击图标固定，再点收起；检查快速进出、Escape、键盘 Tab、窗口失焦与缩放。
3. 检查下载、消息、个人资料的默认/hover/选中/键盘焦点；打开内嵌工具面板时不意外关掉正在使用的入口。
4. 触屏、≤760px 窄屏仅用点击展开；菜单行、按钮触控区域和背景关闭仍可用。
5. 模型、图片、网页分别检查加载、成功、网络失败、超时重试；检查失败后晚到的结果不会覆盖新状态。
6. 大图片、ZIP、Skill 读取期间不能重复提交；取消或更换 Case 后旧读取不恢复草稿。本地模型入口已移除。
7. 复查 3D 手势、文件切换、版本关联和审核动作的联合路径。源码测试不是这些浏览器检查的替代品。
8. 成员搜索点击展开、连续勾选/取消、中文与账号搜索、空结果、点外部/Esc/Tab 关闭；桌面/窄屏检查弹层不裁切、三种角色不截断。
9. 必填信息和 List 完整后创建，在演示账号上下文中确认收件人 inbox、已读、数据单跳转；真实跨账号与飞书投递尚未接入，不能声称已验证。
10. Skill 在桌面/窄屏从平台列表进入“创建 Skill”，分别走填写表单和上传文件；验证读取、修改、移除、失败重试、创建成功后回列表且不自动勾选。检查搜索/多选保留、右侧 查看 详情弹窗、文件选择/拖拽、键盘焦点和长内容。创建成功即同步个人；取消数据单保留已创建 Skill，未发布草稿不入库，取消勾选不删除库记录。
11. 概览交付进度检查供应商 Logo/首字与数据单对应；在桌面/窄屏确认标识不裁切、文字和进度不挤压。替换/移除 Logo 后重新打开概览，图片失败应回退首字；浏览器实测尚未执行。
12. 创建数据单检查独立页面的返回入口、三个纵向区块和固定操作栏；输入未失焦时返回、切换侧栏、打开通知/个人任务，应确认或保留草稿。创建成功进入详情，编辑仍为弹窗；检查审核返回、继续填写/放弃、上传中离开、Skill 创建后取消数据单、刷新离开提醒，以及键盘/窄屏布局。新增手动「保存草稿」与浏览器本地恢复；未保存修改仍由 beforeunload 保护。见 `scripts/DELIVERY_DRAFTS.md`。
13. 模型状态检查概览卡片进入详情和返回焦点、原生供应商/线路筛选、排查详情、长模型/供应商名称及窄屏指标重排。当前没有真实监控数据，默认须为待接入；在授权测试环境注入独立合成快照，确认 100% 可用仍保留异常线路、未知/过期不显示比例、目录冲突与计费来源不一致均有明确提示。浏览器原生展开状态、计时刷新与实际布局尚未验收。
14. 交付预览确认无示例/返回产物/本地模型入口，真实文件仍可预览和下载；历史标题无横线或版本摘要，首轮紧接标题，空历史显示“尚未审核”；待审核候选不在标题旁重复显示，但业务字段、版本关系和审核动作仍保留。

## 源码包

使用 `node scripts/package-claude-handoff.mjs /绝对路径/新的文件名.zip` 生成。脚本仅收集明确目录，拒绝符号链接，不覆盖旧压缩包，包含逐文件 `SOURCE_MANIFEST.sha256`；完成后执行 ZIP 完整性验证。

快照包含已提交与未提交的当前源码，不包含 Git 历史、node_modules、环境密钥、缓存或浏览器截图。旧迁移脚本保留为历史参考，不能整体重放。
