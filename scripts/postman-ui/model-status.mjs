// The canonical model-status templates own the three-tab information architecture.
// This build hook remains for pipeline compatibility and applies no legacy rewrites.
export const modelStatusCopy = [];
export function installModelStatus(t) { return t; }
