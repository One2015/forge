# Forge · UI/UX 优化方向（设计负责人视角）

> 基于 Postman / GitHub / Graphite / Vercel / Deel / Cursor / Neon / Render / Stripe / v0
> 等产品的界面研究，为 Forge 收敛出「精致、一致、状态正确」的设计方向。
> 参照产品的共性：**中性灰打底 + 一个品牌色 + 语义色只给状态**，密度对齐 Postman/Cursor 这类
> 工具台，而非营销型 SaaS。

## 0. 核心诊断

Forge 现在「乱」的根源不是审美，是**没有单一事实来源**：

- 圆角在用 **10 种值**（2/3/4/5/6/7/8/10/12/14px）
- 字号在用 **13 种值**（11–25px）
- 卡片内边距、状态词表、按钮层级各处不一致
- 全是内联 hex，`:root` 里定义的变量基本被绕过

所有下面的方向，第一步都是**把这些收敛成 token**。

---

## 1. 建立设计 token（最高优先级，先做这个）

在 `:root` 里定义、全站只用这套，禁止再写裸值。

### 圆角（10 种 → 4 种）
```
--r-sm: 6px    小控件（chip、tag、小按钮内圆角）
--r-md: 8px    输入框、次级按钮、行内卡片
--r-lg: 10px   卡片、主按钮
--r-xl: 14px   弹窗、大容器
--r-full: 999px pill / 圆形
```

### 字号（13 种 → 6 种，配 Postman 式层级）
```
--fs-caption: 12px  元信息、表头、说明（多为灰色）
--fs-body:    13px  正文、字段值、按钮
--fs-strong:  14px  卡片标题、条目主文
--fs-h3:      17px  弹窗标题、区块标题
--fs-h2:      20px  页面副标题
--fs-h1:      25px  页面主标题
```
等宽字体只给 ID / Run / 代码，永远比正文小一号且更灰。

### 间距（4 的倍数节奏）
```
--sp-1:4  --sp-2:8  --sp-3:12  --sp-4:16  --sp-5:20  --sp-6:24
```
卡片内边距统一 `16–20`，区块间距 `12–16`，页面留白 `24`。

### 颜色（保持现有暖色系，但纪律化）
```
品牌色（唯一强调）  --accent: #b1543a   仅用于：主 CTA、激活态、当前标记、链接强调
动作/链接（中性）  --link: 蓝系          仅用于：查看/打开/次级导航类中性动作
语义色（只给状态）  绿=通过/健康 · 红=失败/危险 · 琥珀=待处理/警告 · 紫=分支/合并 · 灰=停用
```
**关键纪律**：`--accent` 不再到处兼职。「查看完整记录」这类中性跳转用 `--link`（蓝），
把暖色留给真正的主操作和当前状态。装饰性用色一律去掉。

---

## 2. 状态系统正确化（用户重点：tag / toast / 弹窗）

### 2.1 Tag / 状态药丸 —— 一种形状、一套词表

现状问题：12 个状态 key 只对应 8 个标签（4 组重复）；同一「通过」结论有「通过」和
「已通过」两种写法；两个时间线组件用不同徽标词表。

**规范**：
- 全站状态药丸**只有一种形状**：`前置圆点 + 浅色底 + 文字`，圆角 `--r-sm`，`font-size:12px`。
- **一套状态词表**，去掉重复 key，一处定义：

  | 语义 | 文案 | 色 |
  |---|---|---|
  | 排队 | 排队中 | 灰 |
  | 生产 | 生产中 | 琥珀 |
  | 待审 | 待审核 | 琥珀 |
  | 返工 | 返工中 | 红/品牌 |
  | 通过 | 已通过 | 绿 |
  | 交付 | 可交付 / 已交付 | 绿 |
  | 终止 | 已终止 | 灰 |
  | 分支 | 分支 / 已合并 | 紫 |

- **结论词表统一**：一处结论只有一种写法（选「已通过」，删掉「通过」）。
- **复合状态用一枚药丸**（学 Cursor / Cofounder）：`Manual · Active`、`1 scheduled · Active`，
  别把触发方式和状态拆两列。
- **区分两个正交的轴**（已实现，保持）：审核结论（通过/返工，不可撤销）与
  样本标注（good/bad case，可改）永远分开，不共用颜色和形状。
- **元信息 chip ≠ 状态药丸**：`PostgreSQL 15`、`Optional` 这类中性 chip 用灰色无圆点，
  和状态药丸在视觉上区分开。

### 2.2 Toast —— 固定位置、固定结构、固定时长

现状：`reviewToast` 只有一处、样式零散。参照 Postman/Vercel/Cursor：

- **位置固定**：右下角（或全站统一一个角），不随场景变。
- **结构固定**：`状态图标(✓/⚠/✕) + 文案（实体名加粗）+ 可选 Dismiss`。
  例：`✓ 已创建分支「布达拉宫 · 分支 2」· 与主版本共享同一个交付位`。
- **语义色带左边框或图标**：成功=绿、失败=红、提示=中性。
- **时长**：普通 3–4s 自动消失；含撤销/操作的常驻到手动关闭。
- **一句话说清结果**（学 Render）：`已把 X 克隆到 Y，命名为 Z` 比「操作成功」有用。
- **系统级操作也进反馈流**（学 Cursor/Cofounder）：不是静默，也不是弹窗，用带 spinner 的
  行内卡片说明「正在做什么 + 为什么」。

### 2.3 弹窗 / Dialog —— 固定解剖、按钮层级一致

现状：弹窗尺寸不分量级、按钮层级在不同弹窗里相反。参照 Deel/Stripe/Cursor：

- **固定解剖**：`标题 → 一句解释段 → （分隔线）→ 主体 → （分隔线）→ 页脚`。
- **按钮层级全站一致**：次操作（Cancel）文字/描边在左，主操作实心在右。
  - 破坏性主操作 = **红色实心**且单独隔离。
  - 可选步骤里「跳过」就是主操作（学 Vercel）。
- **弹窗分量级**（学 Deel）：
  - 短决策 → 小弹窗（二次确认）
  - 多步流程 → 带编号的中弹窗
  - 真正的长活（如 Reroll 的 Pipeline→数据集→确认）→ **整页向导 + 右栏步骤条**
- **确认弹窗解释「按钮的语义/后果」，而不是问「你确定吗」**（学 Cofounder/PlanetScale）：
  例「设为主版本」先说「原主版本保留为分支、审核结论不变、交付产物将变为 分支2·Run5」。
- **破坏性弹窗列出连带影响**（学 Vercel/删除弹窗）：「这会把 X 从 CRM 移除，相关关联一并清理」。
- **二次确认写入结论后不可撤销**（已实现，保持）：通过审核、要求返工都要二次确认/必填。

### 2.4 空状态 —— 从不留白、从不造假占位

参照 Deel/Graphite/v0：带边框的框里一行灰字说明 + 可选内联链接/主次按钮对。
- 图表也有空状态（插图 + 「暂无数据」），不是空坐标轴。
- 禁止伪造占位行（Forge 曾有的「尚未审核/未开始」假节点已删，保持这个原则）。
- 空状态可自带状态页脚（学 Graphite）：`APPROVALS: Clear · OUTPUTS: None yet`。

---

## 3. 组件一致性

- **按钮**：主=品牌实心 / 次=描边 / 危险=红实心 / 文字按钮=无边框；高度、圆角、内边距统一。
  同一对动作在不同界面（交付弹窗 vs 审核队列弹窗）层级必须一致（已对齐，保持）。
- **卡片**：统一「左标题+说明，右上角操作」，内边距 `16–20`，圆角 `--r-lg`，细灰边框、几乎无填充。
- **表格**：表头大写字距 + 灰色 + 主列带计数徽标；末尾恒有一行幽灵「添加」行（学 Postman）。
  **列数与表头标签必须对齐**（Forge 曾有 6 列声明只给 5 标签导致错位，已修，作为红线）。
- **tab 带内容信号**（学 Postman）：有内容的 tab 挂小圆点（`文件 ●`），未保存挂橙点。
- **数字配比例条/分母**（学 Vercel/Cursor）：成本、配额写成 `已用 / 上限` + 细进度条，
  不是孤零零一个数字（Forge 成本现在就是孤零零的 `$76.30`）。

---

## 4. 精致度（细节层）

- **密度对齐 Postman/Cursor**：紧凑但呼吸感靠留白，不靠边框堆叠；行高、间距守 4 的倍数节奏。
- **左栏壳 + 面包屑**（学 Cursor `‹ Back to Agents`）：扁平 icon+label，激活态浅灰填充。
- **一行说明跟着每个字段/开关**（学 Neon/Cursor）：禁用的开关说明为什么禁用。
- **配置回读成一句话**（学 Cofounder）：设完「1 + 周」，下面出「用户将有 1 周完成本步骤」。
- **微交互**：状态切换、hover、展开用一致的过渡；hover 出操作而非常驻堆按钮。
- **图标统一**：既然已引入 phosphor，全站图标走一套线宽/风格，别混用。

---

## 5. 结构性方向（信息架构层，工作量较大）

- **完整记录页「左事实右门」**（学 Deel 人员详情）：左栏数据卡，右栏一摞导航卡。
- **审核队列的责任分区**（学 Graphite Inbox）：分区名编码「谁被卡住」——待你审核/已退回给你
  vs 等待审核者/等待作者，替代现在的「全部/我的」。
- **运行详情 = 断言级**（学 Postman Run results / Cursor Services+Testing 表）：
  指标条 + 按结果分桶 tab + 每步逐条断言/命令 + N Files Changed，而非只有进度条。
- **每个 case 的模型用量**（用户明确要求，学 Vercel Usage / Cursor `⊙ N Models`）：
  行上标 `⊙ N 模型`，弹窗内展开为「模型 · 调用 · Token · 耗时 · 成本」表，
  拆分维度给「按模型 | 按环节 | 按轮次」，环形量规 + `已用/配额`。
- **分支能力**（已实现基础版，保持并可增强）：行上 diff-stat + `Draft/Branch/Merged` 状态 chip；
  版本对比双列选择器；PlanetScale 式分支树 + 设为主版本。

---

## 6. 建议优先级

| 优先级 | 事项 | 理由 |
|---|---|---|
| **P0** | 建 token（圆角/字号/间距/色）并全量替换裸值 | 一致性的地基，不做这个其它都是打补丁 |
| **P0** | 状态词表去重、结论文案统一、状态药丸单一形状 | 用户点名的「状态正确」核心 |
| **P1** | Toast/弹窗规范化（位置/结构/按钮层级/分量级） | 用户点名，改动局部、收益明显 |
| **P1** | 成本/配额改 `已用/上限` + 每 case 模型用量 | 用户明确要求，且是真实决策信息 |
| **P2** | 运行详情断言级、审核队列责任分区、完整记录左事实右门 | 结构性提升，工作量较大 |

---

*本文件为设计方向纲要，配合 `CLAUDE_CODE_HANDOFF.md`（已实现行为）与 `scripts/`（逐轮机械变更）阅读。*
