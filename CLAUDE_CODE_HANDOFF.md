# Forge 生产与审核 · Claude Code Handoff

## 目标

这是 Forge 生产、审核与交付流程的可交互 IA/UX 原型。当前版本重点收敛主路径：

1. 生产：查看运行状态、停止运行、Reroll。
2. 审核：从审核队列直接进入大弹窗，完成通过或返工。
3. 交付：浏览数据单与 Item，在大弹窗内查看版本关系并直接审核。

线上参考版本：<https://forge-production-review-focus.yvonne112.chatgpt.site/>

## 启动与验证

```bash
npm install
npm run dev
npm run lint
npm run build
```

要求 Node.js `>=22.13.0`。

## 代码结构

- `app/page.tsx`：Sites 页面壳，加载 `/forge.html`。
- `public/forge.html`：完整可交互 Forge 原型与模拟业务数据。
- `scripts/`：按迭代轮次保留的机械变更脚本，便于追溯具体 UX 修复。
- `.openai/hosting.json`：现有 Sites 项目配置。不要未经确认覆盖当前线上链接。

## v8 本轮改动（最新，Codex 交接重点）

本轮以 Postman / GitHub / Graphite / Vercel / Deel 等产品为参照优化审核弹窗，并新增
「从历史版本分支」能力。改动均通过 `scripts/` 下的机械脚本写入 `public/forge.html`：

- **裁决栏只放裁决**：底部只保留“通过审核 / 要求返工”。要求返工为 primary（实心）且在右侧，
  通过审核为 secondary（描边）在左侧；此层级在交付弹窗与审核队列弹窗中一致。
- **下载交付物移出裁决栏**：改到预览区右上角（仅可交付时出现），因为下载不是裁决动作。
  见 `scripts/sample-label-and-download-placement.mjs`。
- **样本标注 good / bad case**：审核弹窗信息面板新增分段控件 `good case | bad case | 未标注`，
  与审核结论正交、通过后仍可改、随时可改。数据存 `state.sampleLabels[itemId]`。
  下游用途：评测集 / few-shot / Pipeline 调优。见同一脚本。
- **从历史版本分支**：审核记录里每一轮都有 `⑂ 从这一版分支`，打开创建分支弹窗
  （分支名 + 必填迭代说明；沿用 Pipeline / 数据集，不继承审核结论）。信息面板显示
  PlanetScale 式分支树（母版挂“主版本”标记，分支挂状态与说明）。数据存
  `state.branches[itemId]`。见 `scripts/branch-from-version.mjs`。
  - 身份与血缘分离：Item ID 不变，血缘用 `parentItemId` + 显示名（如“布达拉宫 · 分支 2”）表达。
  - 交付计数：母版与其全部分支**共享一个交付位**，由“主版本”标记指定占位者，分支不额外计数。
  - 替换主版本 = 移动“主版本”标记；原主版本保留分支身份与旧审核结论（不撤销）。
- **审核弹窗右栏合并为单一滚动区**：原本上下两个独立滚动区会把样本标注 / 分支关系埋在折叠线下；
  现改为 Graphite PR 详情式的单列卡片 + 单滚动区，审核记录标题 sticky，裁决栏 pinned。
  见 `scripts/single-scroll-review-column.mjs`、`scripts/fix-sticky-history-header.mjs`。
- **状态词表清理**：Item 业务状态去掉“未开始 / 待生产”，未跑完的 Item 不进交付子项；
  返工中的 Item 保留真实审核历史与结论文案；审核结果列表改成与待审核一致的卡片
  （修正了原表头 6 列只给 5 标签导致的列错位）；两个审核列表都只显示 title。

## 历史行为（v7 起，仍有效）

- 审核队列的“开始审核”直接打开大尺寸审核弹窗，不再先进入中间详情页。
- 交付 Item 点击后打开大弹窗：左侧 WebGL 预览，右侧 Item 信息、样本标注、分支关系、审核记录。
- 弹窗支持 `↑ / ↓` 切换 Item，`Esc` 关闭。
- 关闭按钮位于弹窗右上角（信息面板一侧）。
- 点击 Item ID 打开该 Item 的完整记录页（含 Pipeline 与数据集），替代原“打开原始网页”。
- “通过审核”必须二次确认后才写入结论。
- “要求返工”必须填写反馈，提交后才更新计数与历史。
- 已通过且未满 20 轮的 Item 支持“追加返工”，用虚线连接件表达，不撤销原结论。
- Reroll 自动继承原 Pipeline、Dataset 与 Item；无更高 Pipeline 版本时明确提示沿用版本。
- 运行中任务使用“停止运行”，停止前确认，停止进度和费用保持冻结。
- Preview 包含加载中、加载较慢、失败、重试、新标签与文件入口。

## 设计与业务约束

- 普通审核者的主路径只有：找到 Item → 看预览与上下文 → 通过或返工。
- 版本树、运行详情、数据集和 Pipeline 是上下文信息，默认降级或按需展开。
- 不增加权限保护；“我的”只是筛选条件。
- 不增加“等待最久”排序。
- 已提交审核结论不可撤销；运行过程允许停止，排队任务允许取消。
- 未关联交付数据单时，通过后的状态是“可交付候选”，不能表述为已进入交付单。
- 分支来源统一表达为“基于第 N 轮审核产物（Run N）”。
- 样本标注（good / bad case）与审核结论正交，不可互相影响，也不进入交付判定。
- 分支与母版共享同一交付位；分支不额外计入数据单目标数；替换主版本仅移动标记、不撤销旧结论。
- Item ID 保持不可变，分支血缘用关系表达（`parentItemId` + 显示名），不得改写 ID。

## 数据与实现边界

- 当前为前端高保真原型，数据与状态主要定义在 `public/forge.html` 中。
- WebGL 预览、文件下载、原始网页链接和运行耗时为模拟数据。
- 若产品化，请优先抽出统一状态模型，避免审核队列、完整记录与交付列表分别推导状态。
- 建议逐步将单文件原型拆为 React 组件，但不要改变当前交互语义或页面密度。

## 回归清单

1. 审核列表任意“开始审核”均打开对应 Item 的大弹窗。
2. 交付列表任意 Item 均能打开弹窗，且上下键切换不会丢失当前筛选。
3. “通过审核”只在二次确认后更新状态与数量。
4. “要求返工”未提交前不更新状态、数量或审核历史。
5. 通知深链能定位目标 Item，不受“我的/全部”筛选影响。
6. Reroll 进入数据集步骤时自动打开 Dataset 并勾选原 Item。
7. Preview 的加载中、慢、失败和无法在线预览状态都有明确恢复动作。
8. 停止运行后进度、节点、费用与耗时不继续跳变。
9. 裁决栏只含“通过审核 / 要求返工”；下载交付物在预览区右上角。
10. good / bad case 标注可切换、通过后仍可改，不影响审核结论与数量。
11. 任一历史轮次的“从这一版分支”可打开创建分支弹窗；创建后信息面板出现分支树。
12. 审核弹窗右栏为单一滚动区，样本标注与分支关系可滚动到，裁决栏始终 pinned。
13. `npm run lint` 与 `npm run build` 均通过。

## Figma

设计文件：<https://www.figma.com/design/EDqbGI75wjzfYg7wdq7c01/Forge?node-id=12-3>

同步范围见 `FIGMA_SCREEN_INVENTORY.md`。
