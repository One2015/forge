# Genova 配色修订 · 2026-09-03

本修订覆盖前两轮蓝色/橙色 CTA 规则。保留 Postman 工作台结构，采用用户截图中的橙色导航、黑色主操作、近白灰色背景及鲜绿/青柠数据色。

- 已更新 14 个主要页面与状态截图，以及审核工作台、数据集详情、Pipeline 编辑器、错误状态、1024px/390px 窄窗。费用页新增 Tokens 配色和 1024px 截图。
- 浏览器验证了数据单导航、编辑弹窗、Tab 焦点与 Enter 保存；费用/Tokens 切换、鼠标数值 tooltip、键盘选择时段、项目筛选和重置正常。
- 费用页 1024px 检查：页面宽度与窗口均为 1024px，宽表采用局部滚动。
- `node --test scripts/test-billing.mjs scripts/test-postman-ui.mjs`：33 项通过。包括业务方法保留、路由隔离、表单保存、费用筛选、聚合、排序、明细与导出逻辑。
- 黑色 CTA 的白字对比度 15.89:1，hover 11.35:1；导航橙字对淡橙底 4.65:1，橙色图标 3.10:1；辅助文字对浅灰底 4.61:1，焦点对白底 4.92:1。浅色图表序列依赖图例、可聚焦数据柱与数值 tooltip，不宣称完成全产品无障碍认证。
- Billing 使用 Postman 橙色阶区分费用、输入、输出与贡献者；模型 Latency 使用青柠基线和橙色异常，hover 仅改变填充，键盘焦点显示深灰 1px 轮廓。当前模型监控没有真实数据，不新增虚构的延迟图。
- 用户原图保存在 `output/postman-ui/references/genova/`，上一轮橙色 CTA 截图保存在 `output/postman-ui/orange-iteration/`，路径均相对外层工作目录。

当前原型新增的费用分析实现已同步进独立预览。仅写入 Postman 变体及其规范，不回写原版，不合并分支，继续使用 3011 端口。


## 中性灰修订 · 2026-09-03

按用户最新反馈，所有基础背景、灰阶文字、边框、hover 和黑色 CTA 去除绿调。selected/highlight 为纯白底，以边框、字重或定位线区分；费用汇总卡片及图表悬停容器均取消浅绿底。数据图形与成功语义标记保留独立色彩。

实际浏览器检查了交付默认/筛选/悬停状态、数据单详情、编辑弹窗、费用与 Tokens；选中标签为 rgb(255,255,255)，表单边框为 rgb(204,204,204)，正文为 rgb(34,34,34)。8 项 Postman 变体业务保留测试通过。


## 全路由一致性复审 · 2026-09-03

补齐 Pipeline 编辑器、模型状态、审核队列/结果、资源、Run 详情、Item 生命周期、创建和错误页的稳定页面钩子与中性工作面规则。类别色只用于状态、数据、节点标记和进度；背景、hover、selected、表头、边框和焦点统一使用中性灰阶。审核结果“全部／我的”现使用同一浅灰分段容器，选中项纯白并带深灰 1px 内描边。关键回归 83 项通过，浏览器复查 17 个代表路由与状态。
